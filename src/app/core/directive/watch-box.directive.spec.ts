import { WatchBoxDirective } from './watch-box.directive';

describe('WatchBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new WatchBoxDirective();
    expect(directive).toBeTruthy();
  });
});
